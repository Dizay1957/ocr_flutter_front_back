name: ocr_flutter
description: PFA Extract Mobile Application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  image_picker: ^1.0.0
  http: ^1.1.2
  path_provider: ^2.1.2
  permission_handler: ^11.1.0
  dio: ^5.4.0
  cupertino_icons: ^1.0.2
  http_parser: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true 